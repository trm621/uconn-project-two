    <article class="post">
      <div class="title">
        <h1>{{title}}</h1>
        <textarea>
            {{content}}
        </textarea>
      </div>
      <div class="meta">
        Posted by {{user.username}} on {{format_date created_at}}
        |
        {{!-- <a href="/post/{{id}}">{{comments.length}} {{format_plural "comment" comments.length}}</a> --}}
        {{!-- need link button to comment page --}}
      </div>
    </article>